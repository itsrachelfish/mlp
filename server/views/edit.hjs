{{> head}}
    {{> header}}

    <div class="viewport">
        <div class="canvas">

        </div>

        <div class="tray"></div>
    </div>

    <div class="templates">
        {{> menu}}
        {{> toolbar}}
        {{> options}}
        {{> timeline}}
        {{> create}}
        {{> object}}
        {{> confirm}}
    </div>
{{> foot}}
