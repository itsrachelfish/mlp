{{> head}}
    {{> header}}

    <div class="viewport">
        <div class="canvas">

        </div>

        <div class="tray"></div>
    </div>

    <div class="templates">
        {{> menu}}
        {{> options}}
        {{> timeline}}
    </div>
{{> foot}}
